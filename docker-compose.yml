version: '3.8'

services:
  web:
    build: .
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 2 --threads 2
    volumes:
      - sqlite_data:/app/data
    env_file:
      - .env
    ports:
      - "8000"

volumes:
  sqlite_data:
