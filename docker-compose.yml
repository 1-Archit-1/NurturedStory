version: '3.8'

services:
  web:
    build: .
    volumes:
      - sqlite_data:/app/data
      - media_data:/app/media
    env_file:
      - .env
    ports:
      - "8000"

volumes:
  sqlite_data:
  media_data:
